# ========== DEV REQUIREMENTS ==========
# This file is for LOCAL DEVELOPMENT only.
# For production/CI use: requirements-prod.txt
#
# Key differences from requirements-prod.txt:
#   - psycopg2-binary (easy install, no system libs needed locally)
#   - pytest + testing tools added
#   - faker added (for fakerdata.py local script)

# ========== CORE DJANGO ==========
Django==6.0.4
django-environ==0.13.0

# ========== DATABASE ==========
# psycopg2-binary for local dev (easy install, no compilation needed)
# Production uses psycopg2 (compiled) — see requirements-prod.txt
psycopg2-binary==2.9.11

# ========== REST API ==========
djangorestframework==3.17.0
django-filter==25.2
drf-spectacular==0.29.0

# ========== AUTHENTICATION ==========
djangorestframework-simplejwt==5.5.1
django-allauth==65.15.0

# ========== STORAGE ==========
django-storages==1.14.6
boto3==1.42.71

# ========== TAGGING ==========
django-taggit==6.1.0

# ========== SERVER ==========
gunicorn==25.1.0
whitenoise==6.12.0

# ========== PASSWORD HASHING ==========
argon2-cffi==25.1.0

# ========== UTILITIES ==========
requests==2.33.1

# ========== SECURITY ==========
django-csp==4.0        # Content-Security-Policy headers — blocks XSS attacks (4.0+ dict format)

# ========== ERROR TRACKING ==========
sentry-sdk==2.55.0

# ========== MONITORING ==========
django-cors-headers==4.9.0

# ========== CODE QUALITY ==========
ruff==0.15.7           # Fast Python linter + formatter (replaces flake8, isort, pyupgrade)

# ========== CACHING ==========
redis==7.3.0
django-redis==6.0.0

# ========== TESTING ==========
pytest==9.0.3
pytest-django==4.12.0
pytest-html==4.2.0
coverage==7.8.0

# ========== DEV TOOLS ==========
Faker==40.11.0         # Used by fakerdata.py (local data seeding script)
