# ========== CORE DJANGO ==========
Django==6.0.6
django-environ==0.13.0

# ========== DATABASE ==========
# psycopg2 (compiled) — NOT psycopg2-binary.
# The binary wheel is not recommended for production: it bundles its own libpq
# which can conflict with system SSL libs and is flagged in security audits.
# deploy.sh installs libpq-dev so compiling works automatically on EC2.
psycopg2==2.9.11

# ========== REST API ==========
djangorestframework==3.17.1
drf-spectacular==0.29.0

# ========== AUTHENTICATION ==========
djangorestframework-simplejwt==5.5.1
django-allauth==65.16.0
cryptography==46.0.7      # Required by django-allauth for Google OAuth JWT handling

# ========== STORAGE ==========
django-storages==1.14.6
boto3==1.42.89

# ========== TAGGING ==========
django-taggit==6.1.0

# ========== SERVER ==========
gunicorn==25.3.0
whitenoise==6.12.0

# ========== PASSWORD HASHING ==========
# argon2-cffi  → primary hasher:  Argon2PasswordHasher
# bcrypt        → fallback hasher: BCryptSHA256PasswordHasher + BCryptPasswordHasher
# Both are listed in settings.py PASSWORD_HASHERS — BOTH must be installed or login breaks
argon2-cffi==25.1.0
bcrypt==5.0.0

# ========== UTILITIES ==========
requests==2.33.1

# ========== SECURITY ==========
django-csp==4.0        # Content-Security-Policy headers — blocks XSS attacks (4.0+ dict format)

# ========== ERROR TRACKING ==========
sentry-sdk==2.58.0

# ========== MONITORING ==========
django-cors-headers==4.9.0


# ========== CACHING (Django built-in RedisCache + redis-py client) ==========
redis==7.4.0
