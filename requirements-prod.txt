# ========== CORE DJANGO ==========
Django==6.0.4
django-environ==0.13.0

# ========== DATABASE ==========
# psycopg2 (compiled) — NOT psycopg2-binary.
# The binary wheel is not recommended for production: it bundles its own libpq
# which can conflict with system SSL libs and is flagged in security audits.
# deploy.sh installs libpq-dev so compiling works automatically on EC2.
psycopg2==2.9.9

# ========== REST API ==========
djangorestframework==3.17.0
django-filter==25.2
drf-spectacular==0.29.0

# ========== AUTHENTICATION ==========
djangorestframework-simplejwt==5.5.1
django-allauth==65.15.0
cryptography==44.0.3      # Required by django-allauth for Google OAuth JWT handling

# ========== STORAGE ==========
django-storages==1.14.6
boto3==1.42.71

# ========== TAGGING ==========
django-taggit==6.1.0

# ========== SERVER ==========
gunicorn==25.1.0
whitenoise==6.12.0

# ========== PASSWORD HASHING ==========
argon2-cffi==25.1.0

# ========== UTILITIES ==========
requests==2.33.1

# ========== SECURITY ==========
django-csp==4.0        # Content-Security-Policy headers — blocks XSS attacks (4.0+ dict format)

# ========== ERROR TRACKING ==========
sentry-sdk==2.55.0

# ========== MONITORING ==========
django-cors-headers==4.9.0

# ========== CODE QUALITY (used in CI lint job — not needed at runtime) ==========
ruff==0.15.7           # Fast Python linter + formatter (replaces flake8, isort, pyupgrade)

# ========== CACHING (ElastiCache / Redis) ==========
redis==7.3.0
django-redis==6.0.0
