from django.apps import AppConfig


class TestappConfig(AppConfig):  # change this line according to your company (rename to match your app e.g. MyAppConfig)
    default_auto_field = "django.db.models.BigAutoField"
    name = "webapp"  # change this line according to your company (update to your app name)
